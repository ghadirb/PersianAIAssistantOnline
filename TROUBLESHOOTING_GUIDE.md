# حل مشکلات - راهنمای مفصل

## 🔴 مشکل 1: "No Transcription method available"

### علت:
- کلیدهای Liara غیر فعال یا ثبت نشده‌اند
- یا Haaniye model موجود نیست (آفلاین)

### راه حل:

#### الف) بررسی کلیدهای Liara
1. برو به: Home Activity → Logcat
2. جستجو کنید: `Liara key found`
3. اگر دیدید `⚠️ No Liara key found` یعنی:
   - فایل keys.txt در gist فرمت صحیح ندارد
   - یا رمزگذاری ناموفق بود

#### ب) فرمت صحیح برای gist
```
فایل: keys.txt (قبل از رمزگذاری)
liara:YOUR_ACTUAL_LIARA_KEY:https://ai.liara.ir/api/69467b6ba99a2016cac892e1/v1
```

#### ج) رمزگذاری درست
```bash
cd C:\Users\Admin\Downloads\key
python encrypt_keys.py keys.txt encrypted.b64 12345
```

#### د) آپلود به gist
1. کل محتوای `encrypted.b64` را کپی کنید
2. رو به: https://gist.github.com
3. New gist
4. Filename: `keys.txt`
5. Content: (پیست کل محتوای encrypted.b64)
6. ذخیره و گرفتن Raw URL

---

## 🔴 مشکل 2: "Offline STT not available"

### علت:
- Haaniye model در assets موجود نیست
- یا copyHaaniye task در build نشد

### راه حل:

#### الف) چک کنید Haaniye موجود است یا نه
1. در پوشه پروژه:
   - `app/src/main/assets/tts/haaniye/` باید موجود باشد
2. اگر موجود نیست:
   - Haaniye fallback فعال نیست

#### ب) Fallback: استفاده از Text Input
- اگر microphone کار نکنید، متن را مستقیم وارد کنید
- صدا ضروری نیست برای چت

---

## 🔴 مشکل 3: پاسخ‌های عجیب AI

### علت:
- کلید Liara رایگان محدودیت‌های شدید دارد
- یا مدل نادرست انتخاب شده

### راه حل:

#### الف) بررسی کدام مدل استفاده می‌شود
1. Logcat جستجو کنید: `Selected Model:`
2. باید دید: `openai/gpt-4o-mini` یا شبیه آن

#### ب) اگر مدل غلط است
- در `BaseChatActivity.kt` تغییر دهید:
```kotlin
AIModel.GPT_4O_MINI  // این مدل برای Liara رایگان درست است
```

#### ج) اگر Liara رایگان محدود است
- درخواست کنید: پنل پولی در Liara
- یا از OpenAI کلید استفاده کنید

---

## 📋 Checklist برای اصلاح

### 1. کلیدهای Liara
- [ ] یک کلید Liara رایگان دارم
- [ ] فایل `keys.txt` صحیح ایجاد کردم
- [ ] با `encrypt_keys.py` رمزگذاری کردم
- [ ] به gist آپلود کردم
- [ ] URL gist درست است

### 2. Build
- [ ] بیلد جدید گرفتم
- [ ] APK نصب کردم
- [ ] App باز کردم

### 3. لاگ‌های مهم
```
✔ Provisioning successful: X keys
✔ Liara key found
✔ Using: Liara (Gemini 2.0 Flash)
```

### 4. تست
- [ ] Home صفحه باز شد و لاگ صحیح نمایش داد
- [ ] ضبط صدا کردم
- [ ] پیام ساده فرستادم

---

## 💡 نکات مهم

### برای Liara رایگان:
- محدودیت کوتاه نمی‌شود
- احتمالاً rate limit دارد
- بهتر است Text Input استفاده کنید

### برای بهتر کار کردن:
1. **Text Input استفاده کنید** - پایدارتر است
2. **صدا برای ویژگی‌های بعدی** - وقتی پنل پولی گیرید
3. **بررسی لاگ‌ها** - همیشه Logcat را ببینید

---

## 📱 اگر هنوز مشکل دارید:

### فعال کردن Desktop Debugging:
```bash
adb logcat -s "HomeActivity,AIClient,SpeechToTextPipeline,AutoProvisioning" -v long
```

### یا در Android Studio:
1. Run → Edit Configurations
2. Logcat فیلتر کنید:
   - HomeActivity
   - AIClient
   - NewHybridVoice

### سپس:
1. Home صفحه باز کنید
2. Logcat رو دنبال کنید
3. بگویید کدام پیام دیدید

---

## ✅ نتیجه انتظار شده:

```
✅ Provisioning successful: 1 keys
✅ Liara key found!
   Key: sk123...
   Base URL: https://ai.liara.ir/api/.../v1

🎤 Using: Liara (Gemini 2.0 Flash) for voice transcription
✅ Online transcription (Gemini 2.0 Flash): سلام
```

اگر این را دیدید = **همه چیز درست کار می‌کند!**
