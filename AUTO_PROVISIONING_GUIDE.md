# راهنمای Auto-Provisioning سیستم کلیدهای API

## 📋 خلاصه تغییرات

این سیستم به طور خودکار کلیدهای API را از Google Drive دانلود، رمزگشایی و فعال می‌کند.

## 🔑 فایل‌های جدید ایجاد شده

1. **AutoProvisioningManager.kt** - مدیر اصلی بارگذاری خودکار
2. **ModelSelector.kt** - انتخاب هوشمند مدل بر اساس کلیدهای موجود
3. **ProvisioningTest.kt** - تست و نمایش وضعیت سیستم

## ⚙️ اولویت‌های سیستم

### اولویت کلیدها:
1. **AIML API** (aimlapi.com) - رایگان
2. **OpenRouter** (openrouter.ai) - رایگان/ارزان  
3. **OpenAI** (openai.com) - پولی
4. **HuggingFace** (huggingface.co) - برای STT (الزامی)

### اولویت مدل‌ها (برای موبایل‌های ضعیف):
1. **Qwen2.5 1.5B** - سریع‌ترین
2. **LLaMA 3.2 1B** - سبک
3. **LLaMA 3.2 3B** - سبک با دقت بهتر
4. **Mixtral 8x7B** - متوسط
5. **LLaMA 3.3 70B** - قدرتمند
6. **GPT/Claude** - پولی (آخرین اولویت)

## 📁 فرمت فایل کلیدها

فایل `api_keys.txt` در Google Drive باید شامل این فرمت باشد:

```
# کلیدهای AIML API (رایگان)
aiml:sk-aiml-xxxxxxxxxxxxxxxxxxxxx
aiml:sk-aiml-yyyyyyyyyyyyyyyyyyyyy

# کلیدهای OpenRouter (رایگان/ارزان)
openrouter:sk-or-xxxxxxxxxxxxxxxxxxxx
openrouter:sk-or-yyyyyyyyyyyyyyyyyyyy

# کلیدهای OpenAI (پولی)
openai:sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxx

# کلیدهای HuggingFace (برای STT - الزامی)
huggingface:hf_xxxxxxxxxxxxxxxxxxxxxxxx
huggingface:hf_yyyyyyyyyyyyyyyyyyyyyyyy

# یا بدون prefix (تشخیص خودکار)
sk-aiml-xxxxxxxxxxxxxxxxxxxxx
sk-or-yyyyyyyyyyyyyyyyyyyyyy
hf_zzzzzzzzzzzzzzzzzzzzzzzz
```

## 🔐 رمزگذاری فایل

برای رمزگذاری فایل از اسکریپت Python استفاده کنید:

```bash
cd C:\Users\Admin\Downloads\key
python encrypt_keys.py api_keys.txt encrypted_keys.txt 12345
```

سپس فایل `encrypted_keys.txt` را در Google Drive آپلود کنید.

## 🚀 نحوه استفاده

سیستم به طور خودکار در ابتدای برنامه اجرا می‌شود:

1. بررسی کلیدهای موجود
2. اگر کلیدی نیست، دانلود از Google Drive
3. رمزگشایی با رمز `12345`
4. پارس و اولویت‌بندی کلیدها
5. ذخیره در PreferencesManager
6. انتخاب بهترین مدل

## 🧪 تست سیستم

برای مشاهده لاگ‌های کامل:

```kotlin
// در MainActivity.onCreate یا جای دیگر
ProvisioningTest.runTest(this)
```

خروجی در Logcat:
```
═══════════════════════════════════════
🧪 شروع تست Auto-Provisioning
═══════════════════════════════════════

📋 کلیدهای موجود: 0
  ⚠️ هیچ کلیدی یافت نشد

🔄 در حال بارگذاری خودکار کلیدها...
✅ موفق: 8 کلید بارگذاری شد
  - AIML: 2 کلید
  - OPENROUTER: 4 کلید (4 فعال)
  - OPENAI: 2 کلید (2 فعال)

🤖 انتخاب بهترین مدل...
  مدل انتخابی: Qwen2.5 1.5B (OpenRouter)
  Provider: OPENROUTER
  توضیحات: خیلی سبک، مناسب گوشی‌های ضعیف‌تر و سریع

📦 مدل‌های قابل استفاده (7):
  1. Qwen2.5 1.5B (OpenRouter)
  2. LLaMA 3.2 1B (OpenRouter)
  3. LLaMA 3.2 3B (OpenRouter)
  4. Mixtral 8x7B (OpenRouter)
  5. GPT-4o Mini (OPENAI)
  6. AIML GPT-3.5 (AIML)
  
✅ تست کامل شد
═══════════════════════════════════════
```

## ⚡ ویژگی‌های کلیدی

### 1. تشخیص خودکار Provider
سیستم می‌تواند از روی pattern کلید، provider را تشخیص دهد:
- `sk-aiml-...` → AIML
- `sk-or-...` → OpenRouter
- `sk-proj-...` → OpenAI
- `hf_...` → HuggingFace

### 2. Fallback هوشمند
اگر یک کلید کار نکرد، خودکار به کلید بعدی می‌رود.

### 3. Model Selection
مدل‌های سبک‌تر اولویت دارند برای موبایل‌های ضعیف.

### 4. STT با HuggingFace
برای تبدیل صوت به متن، کلیدهای HuggingFace الزامی هستند.

## 🔧 تنظیمات

### غیرفعال کردن Auto-Provisioning
```kotlin
val prefsManager = PreferencesManager(context)
prefsManager.setAutoProvisioning(false)
```

### تغییر رمز عبور
در `AutoProvisioningManager.kt`:
```kotlin
private const val DEFAULT_PASSWORD = "رمز_جدید"
```

## 📝 نکات مهم

1. ✅ فایل رمزشده همیشه با Base64 encode است
2. ✅ رمز پیش‌فرض: `12345`
3. ✅ خطوط با `#` یا `//` نادیده گرفته می‌شوند
4. ✅ کلیدهای HuggingFace برای STT الزامی است
5. ✅ سیستم خودکار کلیدهای نامعتبر را غیرفعال می‌کند

## 🐛 عیب‌یابی

### مشکل: کلیدها دانلود نمی‌شوند
- اتصال اینترنت را چک کنید
- دسترسی به Google Drive را بررسی کنید
- File ID را تأیید کنید: `17iwkjyGcxJeDgwQWEcsOdfbOxOah_0u0`

### مشکل: خطای رمزگشایی
- رمز عبور `12345` را تأیید کنید
- فایل با اسکریپت Python رمزگذاری شده است
- فرمت Base64 صحیح باشد

### مشکل: هیچ مدلی انتخاب نمی‌شود
- حداقل یک کلید فعال داشته باشید
- کلیدها را validate کنید
- لاگ‌ها را بررسی کنید

## 📞 پشتیبانی

برای مشکلات و سوالات:
1. لاگ‌های Logcat را بررسی کنید (tag: `AutoProvisioning`)
2. تست سیستم را اجرا کنید: `ProvisioningTest.runTest(context)`
3. فایل کلیدها را بررسی کنید
